{% layout "layout.liquid" %} {% block content %}
<div>
    <a href="/artists/new"><button>Create A Playlist</button></a>
    <a href="/users/logout"><button>Logout</button></a>

  {% for a in artists %}
  
  <article>
    <a href="/artists/{{ a._id }}">
      <h2>
        {{ a.name }}
      </h2>
    </a>
  </article>
  {% endfor %}
</div>
{% endblock %}
